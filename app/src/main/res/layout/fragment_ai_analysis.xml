<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dark_bg"
    android:padding="20dp">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="AI Image Analysis"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvDesc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="Running local neural network convolutional filters to isolate bone density changes at the bifurcation crotch."
        android:textColor="@color/text_grey"
        android:textSize="11sp"
        app:layout_constraintTop_toBottomOf="@+id/tvTitle" />

    <!-- Analysis Window -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardAnalysis"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="24dp"
        app:cardBackgroundColor="@color/dark_surface"
        app:cardCornerRadius="20dp"
        app:layout_constraintBottom_toTopOf="@+id/btnPredict"
        app:layout_constraintTop_toBottomOf="@+id/tvDesc">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/ivXrayImage"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop" />

            <ImageView
                android:id="@+id/ivAnalysisOverlay"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:visibility="gone" />

            <!-- Laser line -->
            <View
                android:id="@+id/viewLaserLine"
                android:layout_width="match_parent"
                android:layout_height="3.5dp"
                android:background="@color/primary_cyan" />

        </FrameLayout>
    </androidx.cardview.widget.CardView>

    <Button
        android:id="@+id/btnPredict"
        android:layout_width="match_parent"
        android:layout_height="54dp"
        android:layout_marginBottom="16dp"
        android:text="Calculate Predictive Risk"
        app:backgroundTint="@color/primary_teal"
        android:textColor="@color/dark_bg"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
